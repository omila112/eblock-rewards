name: eblock_app
description: "A blockchain-based e-waste management system."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^5.1.0
  tflite_flutter:
    path: /Users/omila/Downloads/eblock-rewards/plugins/tflite_flutter_plugin-master
  image: ^4.2.0
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  url_launcher: ^6.3.2
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  typed_data: ^1.3.2

flutter:
  uses-material-design: true
  assets:
    - assets/model_unquant.tflite
    - assets/labels.txt
